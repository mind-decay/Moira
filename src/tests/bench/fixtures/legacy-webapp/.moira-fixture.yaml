name: legacy-webapp
description: Inconsistent Express app with mixed JS/TS and scattered patterns
stack: [javascript, typescript, express]
state: legacy
characteristics:
  - mixed JavaScript and TypeScript
  - inconsistent naming (camelCase and kebab-case)
  - scattered test locations
  - no linting config
  - tech debt markers (TODO/FIXME)
reset_command: "git checkout clean && git clean -fd"
moira_init_preset: express
