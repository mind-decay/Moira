# Architecture: Export to CSV

Add CSV generation to the existing ReportsController. Use a helper function to convert data array to CSV string. Trigger download via Blob URL in the browser.

No new files needed, everything goes in the existing reports module.
