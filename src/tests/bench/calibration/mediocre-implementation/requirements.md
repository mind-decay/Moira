# Requirements: Export to CSV

## Requirements
1. Add export button to reports page
2. Generate CSV from current report data
3. Download file in browser

Note: did not specify handling for large datasets, special characters in data, or date format preferences.
